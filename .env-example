# 本地存储配置示例
# 重命名此文件为.env并填入您的实际配置信息

# 文件上传目录（相对或绝对路径）
# 默认为项目根目录下的uploads文件夹
# 示例: C:\websites\uploads 或 /var/www/uploads
UPLOAD_DIRECTORY=./uploads

# 文件保存期限（天）
# 文件将在指定天数后自动删除
FILE_LIFETIME=1

# 服务器基础URL（用于生成访问链接）
# 本地开发时可留空，生产环境中设置为您的域名
# 例如: https://example.com 或 http://localhost:3030
BASE_URL=http://localhost:3030

# 服务器运行端口
PORT=3030

# 环境设置 (development/production)
NODE_ENV=development 